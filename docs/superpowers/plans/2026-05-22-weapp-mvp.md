# 微信小程�?MVP 实现计划

| **�汾��** | V1.0.0 |
| **״̬** | ����Ч |
| **��������** | 2026-06-15 |

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build complete 微信小程�?MVP with 7 pages (P1-P7) + shared components + API integration

**Architecture:** Native WeChat Mini Program (WXML+WXSS+JS), 7 independent pages under `pages/`, shared components under `components/`, API layer in `services/api.js`. Navigation flows login �?index �?city-picker �?profile �?loading �?preview �?plan. Data flows via `app.globalData` for cross-page state.

**Tech Stack:** WXML, WXSS, JavaScript ES6, WeChat Mini Program SDK (wx.* API), existing backend REST API via `services/api.js`

---

### Task 1: Project Scaffold + Global Config

| **�汾��** | V1.0.0 |
| **״̬** | ����Ч |
| **��������** | 2026-06-15 |

**Files:**
- Modify: `frontend/weapp/app.json`
- Create: `frontend/weapp/app.js`
- Create: `frontend/weapp/app.wxss`
- Create: `frontend/weapp/utils/constants.js`
- Delete: `frontend/weapp/services/api.js` (will be replaced in next task)
- Delete: `frontend/weapp/services/` (will be recreated)

- [ ] **Step 1: Write app.json**

```json
{
  "pages": [
    "pages/login/login",
    "pages/index/index",
    "pages/city-picker/city-picker",
    "pages/profile/profile",
    "pages/loading/loading",
    "pages/preview/preview",
    "pages/plan/plan"
  ],
  "window": {
    "navigationBarTitleText": "AI社保智筹",
    "navigationBarBackgroundColor": "#1A56DB",
    "navigationBarTextStyle": "white",
    "backgroundColor": "#F5F7FA"
  },
  "permission": {
    "scope.userLocation": {
      "desc": "用于自动定位您所在的城市"
    }
  },
  "sitemapLocation": "sitemap.json"
}
```

- [ ] **Step 2: Write app.js (global data + lifecycle)**

```javascript
App({
  globalData: {
    userInfo: null,
    currentCity: null,
    currentCityCode: null,
    planResult: null,
    profileData: null,
  },
  onLaunch() {
    const that = this;
    wx.getSetting({
      success: (res) => {
        if (res.authSetting['scope.userInfo']) {
          wx.getUserProfile({
            desc: '用于完善用户资料',
            success: (res) => {
              that.globalData.userInfo = res.userInfo;
            },
          });
        }
      },
    });
  },
});
```

- [ ] **Step 3: Write app.wxss (global theme styles)**

```css
page {
  background-color: #F5F7FA;
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', sans-serif;
  color: #333;
}
.container {
  padding: 20rpx 30rpx;
}
.card {
  background: #fff;
  border-radius: 16rpx;
  padding: 30rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.06);
}
.btn-primary {
  background: linear-gradient(135deg, #1A56DB, #3B82F6);
  color: #fff;
  border-radius: 48rpx;
  padding: 24rpx 0;
  text-align: center;
  font-size: 32rpx;
  font-weight: 600;
}
.btn-primary:active {
  opacity: 0.85;
}
.text-muted {
  color: #9CA3AF;
  font-size: 26rpx;
}
.text-error {
  color: #EF4444;
  font-size: 26rpx;
}
```

- [ ] **Step 4: Write constants.js**

```javascript
const CITIES = [
  { code: '310000', name: '上海' },
  { code: '110000', name: '北京' },
  { code: '440300', name: '深圳' },
  { code: '440100', name: '广州' },
  { code: '330100', name: '杭州' },
];

const EMPLOYMENT_STATUS = [
  { value: 'employed', label: '企业就业' },
  { value: 'flexible', label: '灵活就业' },
  { value: 'self_employed', label: '自雇' },
  { value: 'unemployed', label: '失业' },
];

const GENDER_OPTIONS = [
  { value: 'male', label: '�? },
  { value: 'female', label: '�? },
];

const POLICY_TYPES = [
  { type: 'subsidy', label: '补贴政策' },
  { type: 'training', label: '培训政策' },
  { type: 'pension', label: '养老保�? },
];

module.exports = { CITIES, EMPLOYMENT_STATUS, GENDER_OPTIONS, POLICY_TYPES };
```

- [ ] **Step 5: Clean up old scaffold**

Remove old `frontend/weapp/services/api.js` (will be replaced in Task 2).

- [ ] **Step 6: Verify structure**

Run: `ls frontend/weapp/` �?should show app.json, app.js, app.wxss, utils/, 7 page directories.

- [ ] **Step 7: Commit**

```bash
git add frontend/weapp/app.json frontend/weapp/app.js frontend/weapp/app.wxss frontend/weapp/utils/constants.js
git add -A frontend/weapp/services/
git commit -m "feat(weapp): scaffold project structure with global config"
```

---

### Task 2: Enhanced API Service

| **�汾��** | V1.0.0 |
| **״̬** | ����Ч |
| **��������** | 2026-06-15 |

**Files:**
- Create: `frontend/weapp/services/api.js` (replace scaffold)
- Delete: `frontend/weapp/services/` old scaffold

- [ ] **Step 1: Write the enhanced api.js with all endpoints + error handling**

```javascript
const BASE_URL = 'http://localhost:30001';
const TOAST_DURATION = 2000;

function showError(msg) {
  wx.showToast({ title: msg, icon: 'none', duration: TOAST_DURATION });
}

function request(method, path, data, userID) {
  return new Promise((resolve, reject) => {
    wx.request({
      url: BASE_URL + path,
      method,
      data,
      header: {
        'x-user-id': userID || 'default',
        'Content-Type': 'application/json',
      },
      success: (res) => {
        if (res.statusCode >= 400) {
          const msg = res.data && res.data.message ? res.data.message : `请求失败(${res.statusCode})`;
          showError(msg);
          reject(new Error(msg));
          return;
        }
        const { code, data: body } = res.data;
        if (code !== 0) {
          const msg = body && body.message ? body.message : '服务异常';
          showError(msg);
          reject(new Error(msg));
          return;
        }
        resolve(body);
      },
      fail: (err) => {
        showError('网络请求失败，请检查网络连�?);
        reject(err);
      },
    });
  });
}

function getProfile(userID) {
  return request('GET', '/v1/profile', null, userID);
}

function updateProfile(userID, data) {
  return request('PUT', '/v1/profile', data, userID);
}

function queryPolicies(userID, { regionCode, policyType, status } = {}) {
  let path = '/v1/policies';
  const params = [];
  if (regionCode) params.push('region_code=' + regionCode);
  if (policyType) params.push('policy_type=' + policyType);
  if (status) params.push('status=' + status);
  if (params.length) path += '?' + params.join('&');
  return request('GET', path, null, userID);
}

function generatePlan(userID, data) {
  return request('POST', '/v1/plans/generate', data, userID);
}

function getPlanDetail(userID, planID) {
  return request('GET', '/v1/plans/' + planID, null, userID);
}

module.exports = { getProfile, updateProfile, queryPolicies, generatePlan, getPlanDetail };
```

- [ ] **Step 2: Commit**

```bash
git add frontend/weapp/services/api.js
git commit -m "feat(weapp): enhanced API service with error handling"
```

---

### Task 3: P1 Login / Authorization Page

| **�汾��** | V1.0.0 |
| **״̬** | ����Ч |
| **��������** | 2026-06-15 |

**Files:**
- Create: `frontend/weapp/pages/login/login.js`
- Create: `frontend/weapp/pages/login/login.wxml`
- Create: `frontend/weapp/pages/login/login.wxss`
- Create: `frontend/weapp/pages/login/login.json`

- [ ] **Step 1: Write login.wxml**

```xml
<view class="container">
  <view class="header">
    <image class="logo" src="/images/logo.png" mode="aspectFit"></image>
    <text class="title">AI社保智筹</text>
    <text class="subtitle">AI驱动的社保规划助�?/text>
  </view>

  <view class="policy-card">
    <checkbox class="policy-checkbox" checked="{{agreed}}" bindchange="onAgreeChange"/>
    <text class="policy-text">我已阅读并同�?/text>
    <text class="policy-link" bindtap="onViewPrivacy">《隐私政策�?/text>
    <text class="policy-text">�?/text>
    <text class="policy-link" bindtap="onViewTerms">《用户协议�?/text>
  </view>

  <button class="btn-primary {{!agreed ? 'btn-disabled' : ''}}" 
          disabled="{{!agreed}}" 
          bindtap="onLogin">
    <text wx:if="{{!loading}}">微信一键登�?/text>
    <text wx:else>登录�?..</text>
  </button>

  <view class="footer">
    <text class="text-muted">登录即表示您同意上述条款</text>
  </view>
</view>
```

- [ ] **Step 2: Write login.wxss**

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 120rpx 60rpx 0;
  min-height: 100vh;
  background: linear-gradient(180deg, #EEF2FF 0%, #F5F7FA 100%);
}
.header {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 80rpx;
}
.logo {
  width: 160rpx;
  height: 160rpx;
  border-radius: 40rpx;
  margin-bottom: 24rpx;
}
.title {
  font-size: 48rpx;
  font-weight: 700;
  color: #1A56DB;
  margin-bottom: 12rpx;
}
.subtitle {
  font-size: 28rpx;
  color: #6B7280;
}
.policy-card {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  background: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-bottom: 48rpx;
  width: 100%;
}
.policy-checkbox {
  margin-right: 12rpx;
}
.policy-text {
  font-size: 24rpx;
  color: #6B7280;
}
.policy-link {
  font-size: 24rpx;
  color: #1A56DB;
  text-decoration: underline;
}
.btn-disabled {
  opacity: 0.5;
}
.footer {
  margin-top: 48rpx;
  text-align: center;
}
```

- [ ] **Step 3: Write login.js**

```javascript
const app = getApp();

Page({
  data: {
    agreed: false,
    loading: false,
  },
  onAgreeChange(e) {
    this.setData({ agreed: e.detail.value.length > 0 });
  },
  onViewPrivacy() {
    wx.showModal({
      title: '隐私政策',
      content: '我们收集您的信息用于社保规划服务...',
      showCancel: true,
    });
  },
  onViewTerms() {
    wx.showModal({
      title: '用户协议',
      content: '欢迎使用AI社保智筹...',
      showCancel: true,
    });
  },
  onLogin() {
    if (!this.data.agreed) return;
    this.setData({ loading: true });
    wx.getUserProfile({
      desc: '用于完善用户资料',
      success: (res) => {
        app.globalData.userInfo = res.userInfo;
        this.getLocationAndNavigate();
      },
      fail: () => {
        this.getLocationAndNavigate();
      },
    });
  },
  getLocationAndNavigate() {
    wx.getLocation({
      type: 'wgs84',
      success: (res) => {
        app.globalData.currentCity = this.getCityName(res.latitude, res.longitude);
        wx.redirectTo({ url: '/pages/index/index' });
      },
      fail: () => {
        wx.redirectTo({ url: '/pages/index/index' });
      },
    });
  },
  getCityName(lat, lng) {
    // MVP: default to 上海; real geocoding in Phase 2
    const { CITIES } = require('../../utils/constants');
    app.globalData.currentCity = CITIES[0].name;
    app.globalData.currentCityCode = CITIES[0].code;
    return CITIES[0].name;
  },
});
```

- [ ] **Step 4: Write login.json**

```json
{
  "navigationBarTitleText": "登录",
  "usingComponents": {}
}
```

- [ ] **Step 5: Commit**

```bash
git add frontend/weapp/pages/login/
git commit -m "feat(weapp): P1 login/authorization page"
```

---

### Task 4: P2 Home Page + P3 City Picker

| **�汾��** | V1.0.0 |
| **״̬** | ����Ч |
| **��������** | 2026-06-15 |

**Files:**
- Create: `frontend/weapp/pages/index/index.js`
- Create: `frontend/weapp/pages/index/index.wxml`
- Create: `frontend/weapp/pages/index/index.wxss`
- Create: `frontend/weapp/pages/index/index.json`
- Create: `frontend/weapp/pages/city-picker/city-picker.js`
- Create: `frontend/weapp/pages/city-picker/city-picker.wxml`
- Create: `frontend/weapp/pages/city-picker/city-picker.wxss`
- Create: `frontend/weapp/pages/city-picker/city-picker.json`

- [ ] **Step 1: Write index.wxml**

```xml
<view class="container">
  <view class="city-bar" bindtap="onCityTap">
    <text class="city-icon">📍</text>
    <text class="city-name">{{currentCity}}</text>
    <text class="arrow">�?/text>
  </view>

  <view class="hero">
    <text class="hero-title">社保规划，智能匹�?/text>
    <text class="hero-desc">AI 为您匹配最优社保方�?/text>
    <button class="btn-primary hero-btn" bindtap="onStartPlan">开始社保筹�?/button>
  </view>

  <view class="section">
    <text class="section-title">政策速览</text>
    <scroll-view class="policy-scroll" scroll-x>
      <view class="policy-card" wx:for="{{policies}}" wx:key="claim_id" 
            data-item="{{item}}" bindtap="onPolicyTap">
        <text class="policy-type">{{item.policy_type}}</text>
        <text class="policy-desc">{{item.subsidy_calc_method}}</text>
      </view>
    </scroll-view>
  </view>
</view>
```

- [ ] **Step 2: Write index.wxss**

```css
.city-bar {
  display: flex;
  align-items: center;
  padding: 20rpx 0;
}
.city-icon { font-size: 32rpx; margin-right: 8rpx; }
.city-name { font-size: 32rpx; font-weight: 600; color: #333; }
.arrow { font-size: 24rpx; color: #9CA3AF; margin-left: 8rpx; }

.hero {
  background: linear-gradient(135deg, #1A56DB, #3B82F6);
  border-radius: 24rpx;
  padding: 60rpx 40rpx;
  margin: 20rpx 0;
  text-align: center;
}
.hero-title { font-size: 40rpx; font-weight: 700; color: #fff; display: block; }
.hero-desc { font-size: 28rpx; color: rgba(255,255,255,0.8); display: block; margin: 16rpx 0 32rpx; }
.hero-btn { margin-top: 16rpx; }

.section { margin-top: 40rpx; }
.section-title { font-size: 32rpx; font-weight: 600; margin-bottom: 20rpx; display: block; }
.policy-scroll { white-space: nowrap; }
.policy-card {
  display: inline-block;
  width: 260rpx;
  background: #fff;
  border-radius: 16rpx;
  padding: 24rpx;
  margin-right: 20rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.06);
}
.policy-type { font-size: 28rpx; font-weight: 600; color: #1A56DB; display: block; }
.policy-desc { font-size: 24rpx; color: #6B7280; margin-top: 8rpx; display: block; }
```

- [ ] **Step 3: Write index.js**

```javascript
const app = getApp();
const api = require('../../services/api');

Page({
  data: {
    currentCity: '加载�?..',
    currentCityCode: '',
    policies: [],
  },
  onShow() {
    const city = app.globalData.currentCity;
    const code = app.globalData.currentCityCode;
    if (city) {
      this.setData({ currentCity: city, currentCityCode: code });
      this.loadPolicies(code);
    } else {
      app.globalData.currentCity = '上海';
      app.globalData.currentCityCode = '310000';
      this.setData({ currentCity: '上海', currentCityCode: '310000' });
      this.loadPolicies('310000');
    }
  },
  loadPolicies(code) {
    const userID = app.globalData.userInfo ? app.globalData.userInfo.nickName : 'default';
    api.queryPolicies(userID, { regionCode: code })
      .then((claims) => {
        this.setData({ policies: claims || [] });
      })
      .catch(() => {});
  },
  onCityTap() {
    wx.navigateTo({ url: '/pages/city-picker/city-picker' });
  },
  onStartPlan() {
    wx.navigateTo({ url: '/pages/profile/profile' });
  },
  onPolicyTap(e) {
    const item = e.currentTarget.dataset.item;
    wx.showModal({ title: item.policy_id, content: item.subsidy_calc_method });
  },
});
```

- [ ] **Step 4: Write index.json**

```json
{
  "navigationBarTitleText": "AI社保智筹",
  "usingComponents": {}
}
```

- [ ] **Step 5: Write city-picker.wxml**

```xml
<view class="container">
  <text class="page-title">选择城市</text>
  <view class="city-list">
    <view class="city-item" wx:for="{{cities}}" wx:key="code" 
          data-code="{{item.code}}" data-name="{{item.name}}" 
          bindtap="onSelect">
      <text class="city-name {{currentCode === item.code ? 'active' : ''}}">{{item.name}}</text>
      <text wx:if="{{currentCode === item.code}}" class="checkmark">�?/text>
    </view>
  </view>
</view>
```

- [ ] **Step 6: Write city-picker.wxss**

```css
.page-title { font-size: 36rpx; font-weight: 700; margin: 20rpx 0; display: block; }
.city-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx 24rpx;
  background: #fff;
  border-radius: 16rpx;
  margin-bottom: 12rpx;
}
.city-name { font-size: 32rpx; color: #333; }
.city-name.active { color: #1A56DB; font-weight: 600; }
.checkmark { font-size: 32rpx; color: #1A56DB; }
```

- [ ] **Step 7: Write city-picker.js**

```javascript
const app = getApp();
const { CITIES } = require('../../utils/constants');

Page({
  data: {
    cities: CITIES,
    currentCode: app.globalData.currentCityCode || '310000',
  },
  onSelect(e) {
    const { code, name } = e.currentTarget.dataset;
    app.globalData.currentCity = name;
    app.globalData.currentCityCode = code;
    wx.navigateBack();
  },
});
```

- [ ] **Step 8: Write city-picker.json**

```json
{
  "navigationBarTitleText": "选择城市",
  "usingComponents": {}
}
```

- [ ] **Step 9: Commit**

```bash
git add frontend/weapp/pages/index/ frontend/weapp/pages/city-picker/
git commit -m "feat(weapp): P2 home page + P3 city picker"
```

---

### Task 5: P4 Profile Form (Multi-Step)

| **�汾��** | V1.0.0 |
| **״̬** | ����Ч |
| **��������** | 2026-06-15 |

**Files:**
- Create: `frontend/weapp/pages/profile/profile.js`
- Create: `frontend/weapp/pages/profile/profile.wxml`
- Create: `frontend/weapp/pages/profile/profile.wxss`
- Create: `frontend/weapp/pages/profile/profile.json`

- [ ] **Step 1: Write profile.wxml**

```xml
<view class="container">
  <view class="step-bar">
    <view class="step {{currentStep >= 1 ? 'active' : ''}}">1</view>
    <view class="step-line {{currentStep >= 2 ? 'active' : ''}}"></view>
    <view class="step {{currentStep >= 2 ? 'active' : ''}}">2</view>
    <view class="step-line {{currentStep >= 3 ? 'active' : ''}}"></view>
    <view class="step {{currentStep >= 3 ? 'active' : ''}}">3</view>
  </view>

  <!-- Step 1: Basic Info -->
  <view wx:if="{{currentStep === 1}}" class="form-step">
    <text class="step-title">基本信息</text>
    <picker mode="selector" range="{{genders}}" bindchange="onGenderChange">
      <view class="form-field">
        <text class="label">性别</text>
        <text class="value">{{gender || '请选择'}}</text>
      </view>
    </picker>
    <view class="form-field">
      <text class="label">年龄</text>
      <input class="input" type="number" placeholder="请输入年�?16-70)" value="{{age}}" bindinput="onAgeInput"/>
    </view>
    <picker mode="region" bindchange="onRegionChange">
      <view class="form-field">
        <text class="label">户籍�?/text>
        <text class="value">{{household || '请选择'}}</text>
      </view>
    </picker>
    <button class="btn-primary" bindtap="onNextStep">下一�?/button>
  </view>

  <!-- Step 2: Employment Info -->
  <view wx:if="{{currentStep === 2}}" class="form-step">
    <text class="step-title">就业信息</text>
    <picker mode="selector" range="{{employmentStatus}}" bindchange="onEmploymentChange">
      <view class="form-field">
        <text class="label">就业状�?/text>
        <text class="value">{{employment || '请选择'}}</text>
      </view>
    </picker>
    <view class="form-field">
      <text class="label">累计社保年限</text>
      <input class="input" type="number" placeholder="请输入年�?�?" value="{{years}}" bindinput="onYearsInput"/>
    </view>
    <view class="btn-row">
      <button class="btn-secondary" bindtap="onPrevStep">上一�?/button>
      <button class="btn-primary" bindtap="onNextStep">下一�?/button>
    </view>
  </view>

  <!-- Step 3: Family & Extra -->
  <view wx:if="{{currentStep === 3}}" class="form-step">
    <text class="step-title">补充信息</text>
    <picker mode="selector" range="{{childOptions}}" bindchange="onChildChange">
      <view class="form-field">
        <text class="label">子女情况</text>
        <text class="value">{{hasChildrenText || '请选择'}}</text>
      </view>
    </picker>
    <view class="form-field">
      <text class="label">月预算（元）</text>
      <input class="input" type="number" placeholder="请输入月缴费预算" value="{{budget}}" bindinput="onBudgetInput"/>
    </view>
    <view class="form-field">
      <text class="label">当前账户余额（元�?/text>
      <input class="input" type="number" placeholder="请输入当前账户余�? value="{{balance}}" bindinput="onBalanceInput"/>
    </view>
    <view class="btn-row">
      <button class="btn-secondary" bindtap="onPrevStep">上一�?/button>
      <button class="btn-primary" bindtap="onSubmit" loading="{{submitting}}">
        {{submitting ? '提交�?..' : '生成方案'}}
      </button>
    </view>
  </view>

  <view wx:if="{{error}}" class="error-msg">{{error}}</view>
</view>
```

- [ ] **Step 2: Write profile.wxss**

```css
.step-bar {
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 30rpx 0;
}
.step {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  background: #E5E7EB;
  color: #9CA3AF;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 600;
}
.step.active { background: #1A56DB; color: #fff; }
.step-line {
  width: 80rpx;
  height: 4rpx;
  background: #E5E7EB;
  margin: 0 12rpx;
}
.step-line.active { background: #1A56DB; }

.form-step { margin-top: 20rpx; }
.step-title { font-size: 36rpx; font-weight: 700; margin-bottom: 32rpx; display: block; }
.form-field {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 28rpx 0;
  border-bottom: 2rpx solid #F3F4F6;
}
.label { font-size: 30rpx; color: #374151; }
.value { font-size: 30rpx; color: #1A56DB; }
.input { font-size: 30rpx; text-align: right; color: #333; }
.btn-row { display: flex; gap: 20rpx; margin-top: 40rpx; }
.btn-secondary {
  flex: 1;
  background: #fff;
  border: 2rpx solid #D1D5DB;
  color: #374151;
  border-radius: 48rpx;
  padding: 24rpx 0;
  text-align: center;
  font-size: 32rpx;
}
.error-msg {
  color: #EF4444;
  font-size: 26rpx;
  text-align: center;
  margin-top: 20rpx;
}
```

- [ ] **Step 3: Write profile.js**

```javascript
const app = getApp();
const api = require('../../services/api');
const { GENDER_OPTIONS, EMPLOYMENT_STATUS } = require('../../utils/constants');

Page({
  data: {
    currentStep: 1,
    genders: GENDER_OPTIONS.map(g => g.label),
    employmentStatus: EMPLOYMENT_STATUS.map(e => e.label),
    childOptions: ['�?, '�?],
    age: '', gender: '', household: '',
    employment: '', years: '',
    hasChildrenText: '', budget: '', balance: '',
    submitting: false, error: '',
  },
  onGenderChange(e) {
    this.setData({ gender: GENDER_OPTIONS[e.detail.value].value });
  },
  onAgeInput(e) {
    this.setData({ age: e.detail.value });
  },
  onRegionChange(e) {
    this.setData({ household: e.detail.value.join(' ') });
  },
  onEmploymentChange(e) {
    this.setData({ employment: EMPLOYMENT_STATUS[e.detail.value].value });
  },
  onYearsInput(e) {
    this.setData({ years: e.detail.value });
  },
  onChildChange(e) {
    this.setData({ hasChildrenText: e.detail.value === 0 ? '�? : '�? });
  },
  onBudgetInput(e) {
    this.setData({ budget: e.detail.value });
  },
  onBalanceInput(e) {
    this.setData({ balance: e.detail.value });
  },
  onNextStep() {
    if (this.data.currentStep < 3) {
      this.setData({ currentStep: this.data.currentStep + 1, error: '' });
    }
  },
  onPrevStep() {
    if (this.data.currentStep > 1) {
      this.setData({ currentStep: this.data.currentStep - 1, error: '' });
    }
  },
  onSubmit() {
    const age = parseInt(this.data.age);
    if (age < 16 || age > 70) {
      this.setData({ error: '年龄必须�?6-70之间' });
      return;
    }
    const budget = parseFloat(this.data.budget);
    if (!budget || budget <= 0) {
      this.setData({ error: '请输入有效月预算' });
      return;
    }
    this.setData({ submitting: true, error: '' });

    const userID = app.globalData.userInfo ? app.globalData.userInfo.nickName : 'default';
    const profileData = {
      age,
      gender: this.data.gender || 'male',
      household_region_code: app.globalData.currentCityCode,
      current_residence_code: app.globalData.currentCityCode,
      employment_status: this.data.employment || 'flexible',
      social_security_years: parseInt(this.data.years) || 0,
      has_children: this.data.hasChildrenText === '�?,
    };

    api.updateProfile(userID, profileData).then(() => {
      app.globalData.profileData = profileData;
      app.globalData.planInput = {
        age,
        gender: this.data.gender || 'male',
        employment: this.data.employment || 'flexible',
        contribution_years: parseInt(this.data.years) || 0,
        current_balance: parseFloat(this.data.balance) || 0,
        monthly_budget: budget,
        local_avg_salary: budget * 2,
      };
      wx.redirectTo({ url: '/pages/loading/loading' });
    }).catch((err) => {
      this.setData({ submitting: false, error: err.message || '提交失败' });
    });
  },
});
```

- [ ] **Step 4: Write profile.json**

```json
{
  "navigationBarTitleText": "信息采集",
  "usingComponents": {}
}
```

- [ ] **Step 5: Commit**

```bash
git add frontend/weapp/pages/profile/
git commit -m "feat(weapp): P4 multi-step profile form"
```

---

### Task 6: P5 Loading Page + P6 Preview Page

| **�汾��** | V1.0.0 |
| **״̬** | ����Ч |
| **��������** | 2026-06-15 |

**Files:**
- Create: `frontend/weapp/pages/loading/loading.js`
- Create: `frontend/weapp/pages/loading/loading.wxml`
- Create: `frontend/weapp/pages/loading/loading.wxss`
- Create: `frontend/weapp/pages/loading/loading.json`
- Create: `frontend/weapp/pages/preview/preview.js`
- Create: `frontend/weapp/pages/preview/preview.wxml`
- Create: `frontend/weapp/pages/preview/preview.wxss`
- Create: `frontend/weapp/pages/preview/preview.json`

- [ ] **Step 1: Write loading.wxml**

```xml
<view class="container">
  <view class="animation-area">
    <view class="spinner"></view>
  </view>
  <text class="loading-text">AI 正在为您匹配最优政�?..</text>
  <text class="loading-sub">{{progressText}}</text>
</view>
```

- [ ] **Step 2: Write loading.wxss**

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 70vh;
}
.animation-area { margin-bottom: 40rpx; }
.spinner {
  width: 100rpx;
  height: 100rpx;
  border: 8rpx solid #E5E7EB;
  border-top-color: #1A56DB;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}
@keyframes spin {
  to { transform: rotate(360deg); }
}
.loading-text { font-size: 32rpx; color: #374151; margin-bottom: 16rpx; }
.loading-sub { font-size: 26rpx; color: #9CA3AF; }
```

- [ ] **Step 3: Write loading.js**

```javascript
const app = getApp();
const api = require('../../services/api');

Page({
  data: {
    progressText: '正在分析您的社保情况...',
  },
  onShow() {
    const userID = app.globalData.userInfo ? app.globalData.userInfo.nickName : 'default';
    const input = app.globalData.planInput;

    if (!input) {
      wx.redirectTo({ url: '/pages/profile/profile' });
      return;
    }

    this.setData({ progressText: '正在计算最优方�?..' });

    api.generatePlan(userID, input).then((result) => {
      wx.setStorageSync('planResult', result);
      setTimeout(() => {
        wx.redirectTo({ url: '/pages/preview/preview' });
      }, 800);
    }).catch(() => {
      this.setData({ progressText: '生成失败，请重试' });
      setTimeout(() => {
        wx.redirectTo({ url: '/pages/profile/profile' });
      }, 2000);
    });
  },
});
```

- [ ] **Step 4: Write loading.json**

```json
{
  "navigationBarTitleText": "方案生成�?,
  "usingComponents": {}
}
```

- [ ] **Step 5: Write preview.wxml**

```xml
<view class="container">
  <text class="page-title">您的社保方案</text>
  <text class="page-desc">以下�?AI 为您推荐的社保方案预�?/text>

  <view class="preview-card">
    <text class="preview-label">预计年补贴额</text>
    <view class="blurred-value">
      <text class="blur-text">{{blurredAmount}}</text>
    </view>
  </view>

  <view class="preview-card">
    <text class="preview-label">月节省额</text>
    <view class="blurred-value">
      <text class="blur-text">{{blurredMonthly}}</text>
    </view>
  </view>

  <button class="btn-primary unlock-btn" bindtap="onUnlock">解锁完整报告</button>

  <view class="tip">
    <text class="tip-text">解锁后可查看方案对比、年度现金流和行动清�?/text>
  </view>
</view>
```

- [ ] **Step 6: Write preview.wxss**

```css
.page-title { font-size: 40rpx; font-weight: 700; text-align: center; margin-top: 40rpx; }
.page-desc { font-size: 28rpx; color: #6B7280; text-align: center; margin: 12rpx 0 40rpx; }
.preview-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 32rpx;
  margin-bottom: 20rpx;
  text-align: center;
}
.preview-label { font-size: 28rpx; color: #6B7280; display: block; margin-bottom: 12rpx; }
.blurred-value {
  filter: blur(12rpx);
  user-select: none;
}
.blur-text { font-size: 48rpx; font-weight: 700; color: #1A56DB; }
.unlock-btn { margin: 40rpx 0; }
.tip { text-align: center; padding: 0 40rpx; }
.tip-text { font-size: 24rpx; color: #9CA3AF; }
```

- [ ] **Step 7: Write preview.js**

```javascript
const app = getApp();

Page({
  data: {
    blurredAmount: '�?¥XX,XXX/�?,
    blurredMonthly: '�?¥XXX/�?,
  },
  onUnlock() {
    const planResult = wx.getStorageSync('planResult');
    if (planResult && planResult.plan_id) {
      app.globalData.planResult = planResult;
      wx.navigateTo({ url: '/pages/plan/plan?planId=' + planResult.plan_id });
    } else {
      wx.showToast({ title: '方案数据异常', icon: 'none' });
    }
  },
});
```

- [ ] **Step 8: Write preview.json**

```json
{
  "navigationBarTitleText": "方案预览",
  "usingComponents": {}
}
```

- [ ] **Step 9: Commit**

```bash
git add frontend/weapp/pages/loading/ frontend/weapp/pages/preview/
git commit -m "feat(weapp): P5 loading + P6 preview pages"
```

---

### Task 7: P7 Plan Detail Page

| **�汾��** | V1.0.0 |
| **״̬** | ����Ч |
| **��������** | 2026-06-15 |

**Files:**
- Create: `frontend/weapp/pages/plan/plan.js`
- Create: `frontend/weapp/pages/plan/plan.wxml`
- Create: `frontend/weapp/pages/plan/plan.wxss`
- Create: `frontend/weapp/pages/plan/plan.json`

- [ ] **Step 1: Write plan.wxml**

```xml
<view class="container">
  <text class="page-title">方案详情</text>

  <scroll-view class="scheme-list" scroll-x>
    <view class="scheme-card {{currentScheme === index ? 'active' : ''}}"
          wx:for="{{schemes}}" wx:key="name" 
          data-index="{{index}}" bindtap="onSchemeTap">
      <text class="scheme-name">{{item.name}}</text>
      <text class="scheme-pension">预计养老金</text>
      <text class="scheme-amount">{{item.projected_pension.toFixed(0)}}�?�?/text>
      <text class="scheme-cost">月缴�? {{item.monthly_cost.toFixed(0)}}�?/text>
    </view>
  </scroll-view>

  <view class="detail-card" wx:if="{{currentSchemeData}}">
    <view class="detail-row">
      <text class="detail-label">缴费基数</text>
      <text class="detail-value">{{currentSchemeData.base_salary}}�?/text>
    </view>
    <view class="detail-row">
      <text class="detail-label">月缴�?/text>
      <text class="detail-value">{{currentSchemeData.monthly_cost.toFixed(0)}}�?/text>
    </view>
    <view class="detail-row">
      <text class="detail-label">企业年补�?/text>
      <text class="detail-value highlight">{{currentSchemeData.annual_subsidy.toFixed(0)}}�?�?/text>
    </view>
    <view class="detail-row">
      <text class="detail-label">预计养老金</text>
      <text class="detail-value highlight">{{currentSchemeData.projected_pension.toFixed(0)}}�?�?/text>
    </view>
  </view>

  <view class="action-section">
    <button class="btn-primary" bindtap="onSavePDF">保存�?PDF</button>
    <button class="btn-secondary" bindtap="onShare">分享方案</button>
  </view>
</view>
```

- [ ] **Step 2: Write plan.wxss**

```css
.scheme-list { white-space: nowrap; margin: 20rpx 0; padding: 10rpx 0; }
.scheme-card {
  display: inline-block;
  width: 320rpx;
  background: #fff;
  border-radius: 16rpx;
  padding: 28rpx;
  margin-right: 20rpx;
  border: 3rpx solid transparent;
}
.scheme-card.active { border-color: #1A56DB; }
.scheme-name { font-size: 28rpx; font-weight: 600; color: #1A56DB; display: block; }
.scheme-pension { font-size: 24rpx; color: #6B7280; display: block; margin-top: 12rpx; }
.scheme-amount { font-size: 40rpx; font-weight: 700; color: #1A56DB; display: block; margin: 8rpx 0; }
.scheme-cost { font-size: 24rpx; color: #6B7280; display: block; }

.detail-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 30rpx;
  margin: 20rpx 0;
}
.detail-row {
  display: flex;
  justify-content: space-between;
  padding: 20rpx 0;
  border-bottom: 2rpx solid #F3F4F6;
}
.detail-label { font-size: 28rpx; color: #6B7280; }
.detail-value { font-size: 28rpx; color: #333; font-weight: 600; }
.detail-value.highlight { color: #059669; }

.action-section { margin-top: 40rpx; }
.action-section .btn-secondary { margin-top: 20rpx; }
```

- [ ] **Step 3: Write plan.js**

```javascript
const app = getApp();
const api = require('../../services/api');

Page({
  data: {
    schemes: [],
    currentScheme: 0,
    currentSchemeData: null,
  },
  onLoad(query) {
    const planId = query.planId;
    if (!planId) {
      wx.showToast({ title: '参数错误', icon: 'none' });
      return;
    }
    const cached = wx.getStorageSync('planResult');
    if (cached && cached.recommended_schemes) {
      this.setData({ schemes: cached.recommended_schemes });
      this.selectScheme(0);
    } else {
      const userID = app.globalData.userInfo ? app.globalData.userInfo.nickName : 'default';
      api.getPlanDetail(userID, planId).then((plan) => {
        if (plan && plan.recommended_schemes) {
          this.setData({ schemes: plan.recommended_schemes });
          this.selectScheme(0);
        }
      }).catch(() => {});
    }
  },
  selectScheme(index) {
    const schemes = this.data.schemes;
    if (schemes && schemes[index]) {
      this.setData({
        currentScheme: index,
        currentSchemeData: schemes[index],
      });
    }
  },
  onSchemeTap(e) {
    this.selectScheme(e.currentTarget.dataset.index);
  },
  onSavePDF() {
    wx.showToast({ title: 'PDF 报告导出（Phase 2�?, icon: 'none' });
  },
  onShare() {
    wx.showShareMenu({ withShareTicket: true });
  },
});
```

- [ ] **Step 4: Write plan.json**

```json
{
  "navigationBarTitleText": "方案详情",
  "usingComponents": {}
}
```

- [ ] **Step 5: Commit**

```bash
git add frontend/weapp/pages/plan/
git commit -m "feat(weapp): P7 plan detail page with scheme comparison"
```

---

### Task 8: Wire Everything + Final Verification

| **�汾��** | V1.0.0 |
| **״̬** | ����Ч |
| **��������** | 2026-06-15 |

**Files:**
- Verify: all files across `frontend/weapp/`

- [ ] **Step 1: Verify file structure**

Check that all 7 pages exist under `frontend/weapp/pages/` and each has .js, .wxml, .wxss, .json.

- [ ] **Step 2: Verify app.json page registration**

Check that all 7 pages are registered in `app.json`'s `pages` array in the correct order.

- [ ] **Step 3: Verify cross-page navigation**

Trace the flow: login �?index �?city-picker �?profile �?loading �?preview �?plan. Ensure each `wx.navigateTo`/`wx.redirectTo` URL matches the registered page path.

- [ ] **Step 4: Verify API imports**

Check that `index.js`, `profile.js`, `loading.js`, and `plan.js` all import from `../../services/api` with correct relative paths.

- [ ] **Step 5: Run a clean check**

```bash
# List all files in the weapp project
Get-ChildItem -Recurse -LiteralPath "frontend/weapp" -Include "*.js","*.wxml","*.wxss","*.json" | ForEach-Object { $_.FullName }
```

- [ ] **Step 6: Commit final cleanup**

```bash
git add -A frontend/weapp/
git commit -m "feat(weapp): complete MVP with all 7 pages"
```
